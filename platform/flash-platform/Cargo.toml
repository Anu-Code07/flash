[package]
name = "flash-platform"
version.workspace = true
edition.workspace = true
description = "Mobile-first platform targets: iOS (UIKit), Android (View), Web (WASM/DOM)"

[dependencies]
flash-ir = { workspace = true }

[features]
default = []
ios = []
android = []
web = []

[dev-dependencies]
flash-span = { workspace = true }
flash-stl = { workspace = true }
